# Home Assistant Secrets — copy this file to secrets.yaml and fill in real values.
# secrets.yaml is gitignored and must NEVER be committed.

# LLM / AI integrations
openai_api_key: "sk-replace-with-your-openai-api-key"

# Notification targets (used in automations)
ha_internal_url: "http://192.168.1.222:8123"
